export const Rules = () => {
  return (
    <div>
      Rules
      <div>
        1. Subfolder limit 10 2. Depth limit 4 3. Folder and Note name should
        start with number 4. Note and folder cannot coexist in same folder
      </div>
    </div>
  );
};
